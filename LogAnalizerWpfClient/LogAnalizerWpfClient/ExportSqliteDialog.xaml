<local:MaterialDesignWindow x:Class="LogAnalizerWpfClient.ExportSqliteDialog"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:local="clr-namespace:LogAnalizerWpfClient"
                            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                            Title="Export SQLite to SQL Server"
                            Height="350" Width="400"
                            WindowStartupLocation="CenterScreen"
                            WindowStyle="None"
                            ResizeMode="CanResizeWithGrip"
                            Background="{DynamicResource MaterialDesignPaper}">

    <Window.Resources>
        <Style x:Key="AnimatedRaisedButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TransformGroup>
                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                        <TranslateTransform Y="0"/>
                    </TransformGroup>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="0" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" To="1.03" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" To="1.03" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" To="-2" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(Button.Effect).(DropShadowEffect.BlurRadius)" To="12" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(Button.Effect).(DropShadowEffect.Opacity)" To="0.4" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" To="0" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(Button.Effect).(DropShadowEffect.BlurRadius)" To="4" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(Button.Effect).(DropShadowEffect.Opacity)" To="0.2" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel>
        <Grid Background="{DynamicResource PrimaryHueMidBrush}" Height="40" DockPanel.Dock="Top" MouseDown="Border_MouseDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Export SQLite to SQL Server"
                       VerticalAlignment="Center"
                       Margin="16,0"
                       FontWeight="Bold"
                       Foreground="White"
                       FontSize="16"/>
            <StackPanel Orientation="Horizontal" Grid.Column="1">
                <Button Style="{StaticResource MaterialDesignToolForegroundButton}" Click="Minimize_Click" Width="40" Height="40">
                    <materialDesign:PackIcon Kind="WindowMinimize"/>
                </Button>
                <Button Style="{StaticResource MaterialDesignToolForegroundButton}" Click="MaximizeRestore_Click" Width="40" Height="40">
                    <materialDesign:PackIcon Kind="WindowRestore"/>
                </Button>
                <Button Style="{StaticResource MaterialDesignToolForegroundButton}" Click="Close_Click" Width="40" Height="40">
                    <materialDesign:PackIcon Kind="Close"/>
                </Button>
            </StackPanel>
        </Grid>

        <StackPanel Margin="20">
            <TextBlock Text="SQLite File:" Foreground="Teal"/>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                <TextBox x:Name="txtSqliteFilePath" Width="250" IsReadOnly="True"/>
                <Button Content="..." Width="30" Margin="5,0,0,0" Click="BrowseSqliteFile_Click"
                        Style="{StaticResource AnimatedRaisedButton}"/>
            </StackPanel>
            
            <TextBlock Text="Server:" Foreground="Teal" Margin="0,10,0,2"/>
            <TextBox x:Name="txtServer" Width="300"/>

            <TextBlock Text="Database:" Foreground="Teal" Margin="0,10,0,2"/>
            <TextBox x:Name="txtDatabase" Width="300"/>

            <TextBlock Text="Username:" Foreground="Teal" Margin="0,10,0,2"/>
            <TextBox x:Name="txtUsername" Width="300"/>

            <TextBlock Text="Password:" Foreground="Teal" Margin="0,10,0,2"/>
            <PasswordBox x:Name="txtPassword" Width="300"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Export" Width="100" Click="Export_Click" Style="{StaticResource AnimatedRaisedButton}" Margin="5"/>
                <Button Content="Cancel" Width="100" Click="Cancel_Click" Style="{StaticResource AnimatedRaisedButton}" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </DockPanel>
</local:MaterialDesignWindow>